{% extends 'base.html.twig' %}

{% block title %}catégories{% endblock %}

{% block body %}
<style>
    .example-wrapper { margin: 1em auto; max-width: 80%; font: 18px/1.5 sans-serif; }
    .example-wrapper code { background: #F5F5F5; padding: 2px 6px; }
</style>

<div class=" pt-10 grid sm:grid-cols-1 md:grid-cols-2 lg:grid-cols-3 2xl:grid-cols-4 gap-4 example-wrapper">

        {% for category in categories %}

           {# <a  href='{{path('subCatalogue',{"slug":category.slug}) }}'> <h1 class=' text-blue-400 underline hover:text-blue-800'>{{category.name}}</h1></a> #}
     
        <div class="p-4">
        <a href='{{path('subCatalogue',{"slug":category.slug}) }}'>
            <div  class="flex items-center cursor-pointer">
                <img class="w-32 h-32 rounded-full m-0 border-4 border-white" src="/uploads/images/{{category.photo}}">
                <h3  class="mx-4">{{category.name}}</h3>
            </div>
        </a>
        </div>



        {% endfor %}


</div>
{% endblock %}
