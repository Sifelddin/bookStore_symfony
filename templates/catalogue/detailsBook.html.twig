{% extends 'admin.html.twig' %}

{% block title %}Book{% endblock %}

{% block body %}
<style>
    .example-wrapper { margin: 1em auto; max-width: 800px; width: 95%; font: 18px/1.5 sans-serif; }
    .example-wrapper code { background: #F5F5F5; padding: 2px 6px; }
</style>

<div class="example-wrapper">

       

            {# <a  href='{{path('subCatalogue',{"slug":category.slug})}}'>   #}
                <h1>{{book.title}}</h1>
                <h1>{{book.price}}</h1>
                <h1>{{book.photo}}</h1>
                <h1>{{book.title}}</h1>
                <h1>{{book.title}}</h1>
                <h1>{{book.title}}</h1>
                <h1>{{book.title}}</h1>
                <h1>{{book.title}}</h1>
                <h1>{{book.title}}</h1>
                <h1>{{book.title}}</h1>
                <h1>{{book.title}}</h1>
            {# </a> #}
     

    


</div>
{% endblock %}

