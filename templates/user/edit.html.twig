{% extends 'admin.html.twig' %}

{% block body %}
	<div class="min-h-fit flex flex-col sm:justify-center items-center pt-6 sm:pt-0 bg-gray-100 col-span-4">
		<div class="flex flex-col w-full sm:max-w-md mt-6 px-6 py-4 bg-white shadow-md overflow-hidden sm:rounded-lg">
			{{ include('user/_form.html.twig', {'button_label': 'Update'}) }}
		</div>
	</div>
{% endblock %}
